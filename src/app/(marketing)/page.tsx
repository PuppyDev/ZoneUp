import HomeLoginButton from "./components/HomeLoginButton";

export default function Home() {
    return (
        <main className="flex flex-col items-center justify-center w-full h-screen text-red-500">
            <p>Hello Build product fast</p>
            <HomeLoginButton />
        </main>
    );
}
